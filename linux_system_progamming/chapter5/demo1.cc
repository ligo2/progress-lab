#include <stdio.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <unistd.h>

int main() {
    // int fd = open("./text1.txt", O_RDONLY);
    // if (fd == -1) {
    //     perror("open hole");
    //     return -1;
    // }
    // printf("%d\n", fd);
    // sleep(50);
    
    return 0;
}